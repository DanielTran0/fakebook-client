const handleErrors = (error) => {
	// TODO
	// if ([500, 401].includes(error.response.status)) {
	// 	setUser({});
	// 	setToken('');
	// 	localStorage.clear();
	// }
	console.log(error);
};

export default handleErrors;
